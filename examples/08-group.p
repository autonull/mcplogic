# Group Theory - Inverse Existence
# In a group, every element has an inverse

# Group axioms for element a:
# 1. e is identity: op(e, a) = a
# 2. a has inverse inv(a): op(a, inv(a)) = e

# Prove: exists x (op(a, x) = e)

op(e, a, a)
op(a, inv(a), e)

op(a, inv(a), e)