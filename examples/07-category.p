# Category Theory - Composition
# If f: A -> B and g: B -> C, then there exists h: A -> C

composition(f, g, h)
domain(f, a)
codomain(f, b)
domain(g, b)
codomain(g, c)
domain(h, a)
codomain(h, c)

composition(f, g, h)